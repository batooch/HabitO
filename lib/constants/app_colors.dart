import 'package:flutter/material.dart';

class AppColors {
  // Hauptfarbe für Buttons, Navigation, Text etc.
  static const Color primaryGreen = Color(0xFF19392A);

  // Standard-Hintergrundfarbe
  static const Color background = Color(0xFFFCF7F1);

  // Weicherer Hintergrund (z.B. für Habit-Karten, Text)
  static const Color backgroundSoft = Color(0xFFFBF5EB);

  //Ori-Vorschläge
  static const Color suggestionCard = Color(0xFFE9F7EF);

  static const Color red = Colors.red;
  static const Color white = Colors.white;
  static const Color black = Colors.black;
}
